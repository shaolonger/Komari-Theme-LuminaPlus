# /compare 多指标分析工作台 Todo

目标：让 `/compare` 支持多台 VPS × 多个指标的同时比较分析，默认以“一眼看懂”的洞察摘要和诊断矩阵呈现结果，同时保留当前单指标趋势/排行体验。

## Todo

- [ ] 1. 梳理现有 `/compare` 数据流、URL 参数和图表结构，确定多指标模式的兼容策略。
- [ ] 2. 新增多指标分析数据模型：支持同一组 VPS 同时构建多个指标序列、风险分、异常标签、综合排行和洞察摘要。
- [ ] 3. 扩展 `/compare` URL 与状态：支持 `metrics=` 多选参数，并提供单指标/多指标模式切换。
- [ ] 4. 设计并实现多指标结果区：洞察摘要、VPS × 指标诊断矩阵、视角切换、行/列聚焦交互。
- [ ] 5. 扩展多指标排行和导出：动态指标列、综合风险排序、Markdown/CSV 导出多指标结果。
- [ ] 6. 补充测试：覆盖多指标序列构建、风险分、洞察摘要、`metrics=` URL 解析和零值 Ping 丢包兼容。
- [ ] 7. 完整验证：运行 typecheck、test、lint、build、release；确认无未提交变更。
- [ ] 8. 发布：更新版本、打 tag、推送 GitHub，并创建新的 GitHub Release。

## 设计原则

- 单指标体验保持不变，多指标作为增强模式进入。
- 结果默认优先回答“谁最差、差在哪、是否同步异常、数据是否可信”。
- 不把不同单位硬叠在一张大图里；用统一风险分驱动颜色和排序，用原始值保留真实语义。
- 支持所有指标自由多选：CPU、内存、磁盘、负载、下行、上行、连接、延迟、丢包。
- 多指标主视图采用诊断矩阵：行是 VPS，列是指标，每个单元格展示风险等级、主数值、最新值、迷你趋势和异常标签。
