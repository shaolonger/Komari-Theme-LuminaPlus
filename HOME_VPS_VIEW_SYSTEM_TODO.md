# Home VPS View System TODO

目标：把首页从“搜索 + 单一分组 Tab”升级为可复用的 VPS 资产视图系统，并为 admin 侧提供可维护的多维标签能力。

## 1. 产品方案固化

- [x] 记录产品目标、交互层级、数据结构和验收标准。
- [x] 明确兼容策略：现有 `group` 继续可用，并作为默认维度参与筛选。

验收：

- 当前文件存在于主题仓库根目录。
- 每个后续任务都能独立实施、验证、提交。

## 2. 主题端数据模型

- [x] 新增首页维度配置、每台 VPS 多维标签、指定展示集合、保存视图的数据类型与默认值。
- [x] 增加 normalize/build/filter 工具，兼容旧 `group`、`region`、`provider`、`business_role`、`tags`。
- [x] 增加单元测试覆盖去重、空值、旧配置兼容、AND 筛选、指定 VPS 强约束。

验收：

- `npm run typecheck` 通过。
- 相关 `vitest` 单元测试通过。

## 3. 首页体验

- [ ] 搜索框文案调整为“搜索 VPS、UUID、备注或标签”。
- [ ] 新增“选择 VPS”面板，支持搜索、勾选、全选当前结果、清空、取消指定。
- [ ] 新增已生效条件 chips，支持单项移除和清空筛选。
- [ ] 将 `home-group-tabs` 升级为当前维度下的分组 Tabs，支持厂商、地区、线路、用途、自定义、旧分组。
- [ ] 支持首页保存视图切换，并让视图保存指定 VPS、维度筛选、排序和默认分组维度。

验收：

- 无筛选时行为与旧首页一致。
- 指定 VPS 后，搜索、风险筛选、排序都只在指定集合内生效。
- 维度 Tabs 可在多个维度间切换，Tab 数量不再受单一 `group` 限制。

## 4. 主题管理页

- [ ] 新增“VPS 标签与视图”管理面板。
- [ ] 支持按 VPS 编辑多维标签：厂商、地区、线路、用途、自定义。
- [ ] 支持维度排序、显示开关、默认维度设置。
- [ ] 支持创建、重命名、删除保存视图，并选择视图包含的 VPS 与筛选条件。
- [ ] 保留现有“分组排序”的兼容入口，提示其为旧分组顺序。

验收：

- 保存后刷新页面配置不丢失。
- 管理页可通过搜索快速定位 VPS。
- 旧 `homeGroupOrder` 配置继续生效。

## 5. komari 后端支撑

- [ ] 在 `/Users/shaolong/Code/personal/komari` 中为 VPS 增加结构化多维标签存储或轻量配置接口。
- [ ] 提供 admin API：读取、批量更新、按 UUID 更新 VPS facets。
- [ ] 保证旧 `group`、`provider`、`business_role`、`tags` 字段仍可作为默认标签来源。
- [ ] 增加 Go 测试覆盖 normalize、保存、读取和非法 payload。

验收：

- `go test ./...` 至少覆盖新增模块和相关 API。
- 现有 client 编辑接口不因标签能力回归。

## 6. 发布

- [ ] 跑主题端测试、类型检查、构建或打包。
- [ ] 跑 komari 端测试。
- [ ] 每个已完成任务独立提交。
- [ ] 主题版本号升级。
- [ ] 创建新 tag。
- [ ] 使用 `gh` 推送到 GitHub 并创建 GitHub Release。

验收：

- 本地 tag 存在并已推送。
- GitHub Release 创建成功。
