# VPS List View TODO

目标：在首页现有“大卡片 / 小卡片”之外新增高密度 VPS 列表视图；在不产生页面横向滚动的前提下展示关键运维信息，并让所有可见数据字段支持单字段、多字段和实时动态排序。

## 1. 方案固化

- [x] 明确列表信息架构、响应式断点和视觉风格。
- [x] 明确所有可见字段均可排序，并支持升序、降序、取消排序。
- [x] 明确支持多级排序、排序优先级调整以及保存视图兼容。
- [x] 明确动态指标随实时数据更新，交互期间冻结行顺序。

验收：

- 当前文件存在于主题仓库根目录。
- 后续任务可逐项实施、验证、提交。

## 2. 三种视图模式

- [x] 将节点视图类型扩展为 `large / compact / list`，兼容已有主题配置和 session override。
- [x] 首页快捷入口提供明确的三选一视图切换，不使用含义不清的循环切换。
- [x] 主题管理页的桌面端、移动端默认视图均增加“列表”选项。
- [x] 增加类型、配置 normalize 与视图状态测试。

验收：

- 旧配置仍按原来的大卡片或小卡片显示。
- 桌面端和移动端可以分别设置列表为默认视图。
- 临时切换只影响当前设备对应的 session override。
- 相关单元测试、类型检查通过。

## 3. 高密度响应式列表

- [x] 新增独立的列表容器和列表行组件，复用现有节点、流量、Ping、费用与到期展示模型。
- [x] 每条记录展示状态、名称/地区/分组/厂商、系统、CPU、内存、磁盘、负载、上下行速率、流量额度、Ping/丢包、在线时长、到期和续费价格。
- [x] 延续现有颜色 token、玻璃表面、微型进度条、离线红色语义和详情入口。
- [x] 桌面宽屏保持一台 VPS 一行；中窄屏合并指标组；移动端允许记录内部换行但不隐藏关键数据。
- [x] 列表表头可吸顶，列表行支持键盘访问和清晰的 hover/focus 状态。

验收：

- 1720px、1024px、720px、375px 宽度下页面均无横向滚动。
- 桌面行高明显低于小卡片，一屏可展示更多 VPS。
- 浅色、深色、玻璃背景和离线状态均保持可读。
- 节点详情链接和辅助提示可正常使用。

## 4. 全字段自定义排序

- [x] 为所有展示字段定义统一、类型安全的排序键和推荐初始方向。
- [ ] 表头各字段均可点击排序，支持“未参与 → 推荐方向 → 相反方向 → 未参与”的三态切换。
- [ ] 支持普通点击设为唯一主排序、`Shift + 点击`追加多级排序，并显示优先级编号。
- [ ] 新增排序面板，支持移动端添加条件、调整优先级、切换方向、移除条件和恢复默认排序。
- [ ] 支持状态、名称、分组、地区、厂商、权重、CPU、内存、磁盘、负载、上传、下载、流量已用/剩余/比例/额度、Ping、丢包、在线时长、最后上报、到期、剩余天数、续费价格、风险和资料完整度排序。
- [ ] 实时指标更新时动态重排；鼠标悬停、键盘焦点或触摸操作期间冻结顺序，结束后恢复。
- [ ] 离线实时指标、空值和无样本数据统一置后；同值使用权重、名称、UUID 保证稳定。

验收：

- 每一个列表中展示的数据字段都有可发现的排序入口。
- 单字段和多字段升降序结果正确且稳定。
- 排序字段随实时数据变化更新，用户操作时行不会突然移走。
- 键盘和移动端无需 `Shift` 也能完成全部排序配置。
- 排序纯函数测试覆盖推荐方向、空值、离线、多级优先级和兜底顺序。

## 5. 保存视图与兼容

- [ ] 保存视图支持持久化完整排序条件数组及方向。
- [ ] 旧 `sortKey` 自动迁移为单个排序条件，保持既有视图行为。
- [ ] 首页选择保存视图时恢复筛选、指定 VPS、分组维度和多级排序。
- [ ] 主题管理页可编辑保存视图的多级排序条件。

验收：

- 保存后刷新页面排序配置不丢失。
- 旧主题设置和旧保存视图无需手动迁移。
- 非法或已删除字段会被安全忽略并回退到默认排序。

## 6. 质量检查

- [ ] 运行全量单元测试、lint、类型检查和生产构建。
- [ ] 检查大卡片、小卡片视图没有回归。
- [ ] 检查各响应式断点、排序交互、空状态、加载状态和离线状态。
- [ ] 修复检查中发现的问题并独立提交。

验收：

- `npm run test` 通过。
- `npm run lint` 通过。
- `npm run typecheck` 通过。
- `npm run build` 通过。

## 7. 发布

- [ ] 更新主题版本号和 release notes。
- [ ] 执行 release 打包并核对 zip、preview 与 manifest 版本。
- [ ] 标记全部 TODO 完成并提交发布变更。
- [ ] 创建新 tag，推送提交和 tag 到 GitHub。
- [ ] 使用 `gh` 创建 GitHub Release 并上传发布产物。

验收：

- 本地与远程 tag 一致。
- GitHub Release 已发布且包含对应版本安装包和预览图。
- 工作区干净，GitHub `main` 与本地发布提交一致。
